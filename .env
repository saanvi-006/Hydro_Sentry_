# HydroSentry Frontend — Environment Configuration
# Backend API URL — Leave empty for same-origin Vercel / Vite reverse proxy to eliminate CORS
VITE_API_BASE_URL=

# Set to 'true' to use the live backend by default, 'false' for mock/demo data
VITE_USE_LIVE_API=true
